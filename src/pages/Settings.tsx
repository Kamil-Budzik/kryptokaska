// components
import Wrapper from '../components/Wrapper';

function Settings() {
  return (
    <Wrapper>
      <header>
        <h1>Settings</h1>
      </header>
    </Wrapper>
  );
}

export default Settings;
